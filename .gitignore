node_modules/
.next/
out/
.env
.env.local
.DS_Store
design/mypromo-screens.html

# copied out of node_modules by scripts/copy-pdfjs.mjs
public/pdfjs/

# copied out of node_modules by scripts/copy-tesseract.mjs
public/tesseract/
duel-*.png

# 4.7 MB of design exploration shots — the concepts we looked at and
# dropped on the way to زيتون. Kept on disk, not in history.
palettes/

# Stamped by scripts/native-url.mjs at sync time — it carries the
# deployment URL, which belongs in the environment, not in history.
android/app/src/main/assets/capacitor.config.json
ios/App/App/capacitor.config.json

# Generated by @capacitor/assets from assets/icon.png
# (kept: the store needs them reproducible)
